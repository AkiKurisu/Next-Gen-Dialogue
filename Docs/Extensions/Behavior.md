# Behavior Extension Explanation
Behavior extension contains some example behaviors for common-use unity api

## Show Notice
These node need to be connected to ``BehaviorModule`` such as ``PreUpdateModule`` and ``CallBackModule``.